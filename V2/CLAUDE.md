# Pointer — do not edit

The single source of truth is `../CLAUDE.md`. Read that file, not this one.
These rules are never duplicated into V2; a second copy drifts and the stale
copy is the one that gets followed.
